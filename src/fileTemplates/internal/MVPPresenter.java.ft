#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME};
#end
/**
 * Author: ${USER}
 * Created on ${DATE}
 */
public class ${NAME}Presenter extends ${NAME}Contract.${NAME}BasePresenter{

}